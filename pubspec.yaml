name: zap_picker
description: powerful package for pick media and files from gallery or file manager
version: 0.0.1
homepage: https://github.com/saifmalsheref/zap_picker
repository: https://github.com/saifmalsheref/zap_picker


environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

platforms:
  android:
